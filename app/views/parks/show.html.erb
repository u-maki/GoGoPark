<!DOCTYPE html>
<html>
<head>
  <title><%= @park&.name || @google_park_data&.dig('name') || '公園詳細' %></title>
  <style>
    .comments {
      margin-top: 20px;
    }

    .comment {
      border: 1px solid #ddd;
      padding: 10px;
      margin-bottom: 10px;
    }

    .no-comments {
      color: #555;
      font-style: italic;
    }

    .photo {
      text-align: center;
      margin-bottom: 20px;
    }

    .photo img {
      max-width: 100%;
      height: auto;
      border-radius: 5px;
    }

    .comment-button {
      background-color: #4CAF50;
      color: white;
      padding: 10px 20px;
      text-align: center;
      border: none;
      cursor: pointer;
      border-radius: 5px;
      margin-top: 20px;
    }

    .comment-button:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <h1><%= @park&.name || @google_park_data&.dig("name") || "公園詳細" %></h1>
<p><%= @park&.address || @google_park_data&.dig("formatted_address") || "住所情報がありません" %></p>

<% if @google_park_data && @google_park_data["photos"] %>
  <img src="https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference=<%= @google_park_data["photos"].first["photo_reference"] %>&key=<%= ENV['GOOGLE_MAPS_API_KEY'] %>" alt="公園の写真">
<% end %>
<h2>投稿されたコメント</h2>
  <div class="comments">
    <% if @comments.present? %>
      <% @comments.each do |comment| %>
        <div class="comment">
          <p><strong><%= comment.user.name %></strong>: <%= comment.content %></p>
        </div>
      <% end %>
    <% else %>
      <p>まだ投稿がありません！是非、投稿お願いします♡</p>
    <% end %>
  </div>

<% if @park %>
  <!-- Railsルートに基づいてpark_idを利用 -->
  <%= link_to '投稿する', new_park_comment_path(@park), class: 'btn btn-primary' %>
<% elsif @google_park_data %>
  <!-- Google Placesのplace_idを利用 -->
  <%= link_to '投稿する', new_comment_path(place_id: params[:place_id]), class: 'btn btn-primary' %>
<% else %>
  <p>公園情報が見つかりません。</p>
<% end %>



</body>
</html>

  
