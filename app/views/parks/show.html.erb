<!-- app/views/parks/show.html.erb -->
<!DOCTYPE html>
<html>
<head>
  <title><%= @park&.name || @google_park_data['name'] %></title>
  <style>
    .comments {
      margin-top: 20px;
    }

    .comment {
      border: 1px solid #ddd;
      padding: 10px;
      margin-bottom: 10px;
    }

    .no-comments {
      color: #555;
      font-style: italic;
    }
  </style>
</head>
<body>
  <h1><%= @park&.name || @google_park_data['name'] %></h1>
  <p><%= @park&.address || @google_park_data['formatted_address'] %></p>

  <h2>投稿されたコメント</h2>
  <div class="comments">
    <% if @park && @comments.any? %>
      <% @comments.each do |comment| %>
        <div class="comment">
          <p><strong><%= comment.user.name %></strong>: <%= comment.content %></p>
        </div>
      <% end %>
    <% elsif @google_park_data %>
      <div class="no-comments">
        <p>この公園に関する情報はまだ投稿されていません。</p>
        <p>Googleからの情報:</p>
        <p><%= @google_park_data['name'] %></p>
        <p><%= @google_park_data['formatted_address'] %></p>
        <p><a href="<%= @google_park_data['url'] %>" target="_blank">Google Mapsで詳細を見る</a></p>
      </div>
    <% else %>
      <p>情報を取得できませんでした。</p>
    <% end %>
  </div>
</body>
</html>
